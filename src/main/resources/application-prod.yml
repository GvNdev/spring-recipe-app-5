spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stmc_prod
    username: stmc_prod_user
    password: stmc
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
    database: mysql
    show-sql: false